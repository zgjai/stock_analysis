{% extends "base.html" %}

{% block title %}服务器错误 - 股票交易记录系统{% endblock %}

{% block content %}
<div class="container-fluid d-flex align-items-center justify-content-center" style="min-height: 60vh;">
    <div class="text-center">
        <div class="error-code mb-4">
            <h1 class="display-1 fw-bold text-danger">500</h1>
        </div>
        <div class="error-message">
            <h2 class="h4 mb-3">服务器内部错误</h2>
            <p class="text-muted mb-4">抱歉，服务器遇到了一些问题。我们正在努力修复中。</p>
            <div class="error-actions">
                <a href="{{ url_for('frontend.dashboard') }}" class="btn btn-primary me-2">
                    <i class="bi bi-house"></i>
                    返回首页
                </a>
                <button type="button" class="btn btn-outline-secondary" onclick="location.reload()">
                    <i class="bi bi-arrow-clockwise"></i>
                    刷新页面
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}